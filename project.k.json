{
  "type": "Project",
  "model_version": 1,
  "name": "autopilot-selftest",
  "description": "Selftest repo dogfooding autopilot via cross-repo uses: aggregates the root selftest spec and the vendored autopilot spec so both constraint suites run together.",
  "specs": {
    "selftest": {
      "spec_dir": ".",
      "envs": {}
    },
    "autopilot": {
      "spec_dir": "autopilot",
      "envs": {
        "PROJECT_ROOT": "autopilot",
        "CHECKOUT_VER": "actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd"
      }
    }
  }
}